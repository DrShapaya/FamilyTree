{
  "name": "familytree",
  "version": "1.0.0",
  "description": "Local-first family tree editor with optional multi-device sync.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "tunnel": "cloudflared tunnel --url http://127.0.0.1:8765"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
